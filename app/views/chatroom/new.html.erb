<<<<<<< HEAD
<div class="overlay"></div>
<header class="header">
  <div class="header-inner">
    <p id="room-open">room</p>
    <div class="gear"><%= image_tag 'gear.png',class:'gear-open'%></div>
  </div>
</header>

  <div class="room-modal">
    <%= image_tag 'close.png' ,class:'room-close-btn' %>
  </div>

  <div class="gear-modal">
    <%= image_tag 'close.png' ,class:'gear-close-btn' %>
    <p><%= link_to "プロフィール", class:"gear-txt" %></p>
    <p><%= link_to "ログアウト", class:"gear-txt" %></p>
  </div>

<div id="your-container">
    <div class="bms-message-container" id="messages">
      <% @messages.each do |message| %>
          <%= render 'message', message: message %>
      <% end %>
      <div class="bms_send">
        <button class="bms_stamp_btn">スタンプ</button>
          <%= form_with model: @message, url: create_path, method: :post, local: false, class: 'new_message' do |f| %>
            <%= f.text_area :content, class: 'bms_send_message' %>
            <%= f.submit '送信', class: "bms_send_btn" %>
        <% end %>
        </div>
        <div class="stamp-container">
         <%= image_tag 'close.png', class:'stamp-close-btn close' %>
            <div class="stamp-container-inner">
                <%= render 'stamp', current_user: current_user %>
            </div>
        </div>
    </div>
</div>


=======
<h1>チャットルーム</h1>
<p>こんにちは、<%= @current_user.username %> さん</p>

  <%= image_tag current_user.avatar.url, class: "icon" %>
<div id="messages">
  <% @messages.each do |message| %>
    <p>
      <%= image_tag message.user.avatar.url, alt: "#{message.user.username}のプロフィール画像", class: "icon" %>
      <strong><%= message.user.username %>:</strong>
      <%= message.content %>
    </p>
  <% end %>
</div>

<div class="text-field">
  <%= form_with model: @message, url: create_path ,method: :post, local: true do |f| %>
  <div>
    <%= f.text_area :content %>
  </div>
  <%= f.submit '送信' %>
<% end %>
</div>



<div class="err-msg">
  <!-- エラーメッセージの表示 -->
  <% if flash[:alert] %>
    <div class="alert">
      <%= flash[:alert] %>
    </div>
  <% end %>
  <% if flash[:notice] %>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
</div>

  <%= form_with model: @current_user, url: user_path(@current_user), method: :patch, local: true, html: { id: 'avatar-update-form', enctype: 'multipart/form-data' } do |form| %>
    <div class="field">
      <%= form.label :avatar, "プロフィール画像を変更" %><br>
      <%= form.file_field :avatar, required: true %>
    </div>

    <div class="actions">
      <%= form.submit "画像を更新" %>
    </div>
  <% end %>
</div>
>>>>>>> origin/session
